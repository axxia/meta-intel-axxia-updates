meta-intel-axxia-updates
========================

OpenEmbedded/Yocto layer containing metadata with minimal changes
to fix older Intel Axxia releases (meta-intel-axxia layer).

Dependencies
============

This layer depends on meta-intel-bsp and meta-intel-distro layers
and their dependencies.

Usage
=====

For each older Intel Axxia release there is a branch with updates.
Choose the proper relase branch and add this layer in conf/bblayers.conf
from the Intel Axxia build.

$ bitbake-layers add-layer -F <path-to-this-layer>

